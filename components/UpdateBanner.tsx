'use client';

// Auto-reload on new deploy was disabled: self-reloads interrupted the session
// and dropped the wallet connection. Refresh manually to pick up new versions.
export function UpdateBanner() {
  return null;
}
